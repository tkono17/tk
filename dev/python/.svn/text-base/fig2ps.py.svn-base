#!/usr/bin/env python

import os
import sys

fname_out = 'fig.tex'

texfile = open(fname_out, 'w')

def usage():
    print "Usage: " % (sys.argv[0])

if __name__=='__main__':
    if len(sys.argv)!=3:
        usage()
        sys.exit(0)
    fname_out = sys.argv[2]
